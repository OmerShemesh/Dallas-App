import { SearchSetupPipe } from './search-setup.pipe';

describe('SearchSetupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSetupPipe();
    expect(pipe).toBeTruthy();
  });
});
